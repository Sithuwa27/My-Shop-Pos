const config = {
  appId: 'com.brave.mobilepos',
  appName: 'My POS',
  webDir: 'dist',
  bundledWebRuntime: false,
  plugins: { SplashScreen: { launchAutoHide: true, backgroundColor: '#020617', showSpinner: false } }
};
export default config;
